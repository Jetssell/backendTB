package pe.visionary.tb.service;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * ⚠️ Plantilla de prueba unitaria.
 * Reemplaza nombres de clase/métodos para apuntar a TU CalculoService real.
 */
@SpringBootTest
class CalculoServiceTest {

    // @Autowired
    // private CalculoService calculoService;

    @Test
    @Disabled("TODO: vincular con tu servicio real y habilitar")
    @DisplayName("Calcular agregados por sexo y edad dentro del rango de fechas")
    void testCalcularPorSexoYEdad() {
        // var resp = calculoService.calcularPorSexoYEdad(LocalDate.parse("2024-01-01"), LocalDate.parse("2024-12-31"));
        // assertNotNull(resp);
        // assertTrue(resp.getConfirmados() >= 0);
        // assertTrue(resp.getNoConfirmados() >= 0);
    }
}
